#include <stdlib.h>
#include <string>
#include <cstring>
#include <iostream>

using namespace std;

int main(){
	int n,k;
	while(scanf("%d %d",&n,&k)){
		if (n==0 && k==0){
			break;
		}
		std::istream str;
		cin >> str;
	}
}
