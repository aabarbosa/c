#include <iostream>
using namespace std;

// main() is where program execution begins.
int main() {
   cout << "This is very helpful when \n"; 
   cout << "the editor do not work as the manual says.";
   cout << "\nHow much of a beginner this tool have is";
   cout << " comparable to the size of the universe"; //You write down 
   return 0;
}